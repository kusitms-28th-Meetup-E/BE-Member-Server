server:
  port: 8083

spring:
  application:
    name: MemberService

  h2:
    console:
      enabled: true

#  main:
#    allow-bean-definition-overriding: true



  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://local.cjdhyv98eoki.ap-northeast-2.rds.amazonaws.com:3306/gwang-jang?autoReconnection=true;sendStringParametersAsUnicode=false?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8

    hikari:
      pool-name: jpa-hikari-pool
      maximum-pool-size: 5
      jdbc-url: jdbc:mysql://local.cjdhyv98eoki.ap-northeast-2.rds.amazonaws.com:3306/gwang-jang?autoReconnection=true;sendStringParametersAsUnicode=false?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
      username: admin
      password: helloworld
      driver-class-name: com.mysql.cj.jdbc.Driver
      data-source-properties:
        rewriteBatchedStatements: true

  jpa:
    database: mysql
    hibernate: # hibernate 사용 설정

      # 애플리케이션 실행 시점에 테이블을 다 지우고, 내가 가진 entity 정보를 보고 다시 테이블 자동 생성
      ddl-auto: update
      generate-ddl: true
    properties: # property 사용 설정
      hibernate: # hibernate property 설정
        format_sql: true
        use_sql_comments: true
    show-sql: false
    open-in-view: true

  data:
    redis:
      port: 6379
      host: gwangjang.36k9ap.ng.0001.apn2.cache.amazonaws.com


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

logging:
  level:
    com.example.memberservice: DEBUG



greeting:
  message: Welcome to the Simple E-commerce.

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans
kakao:
  client:
    id: c1b38a40e2bba2be6a4fd18a38179d5f
    redirect-uri: http://localhost:8888/member/auth
    token-uri: https://kauth.kakao.com/oauth/token
    secret: ZkcLZF7NVt2h45ip2zXxj6QaRJisKeSu

app-id: #수정해야함
  kakao: 983681
  google: ---

jwt:
  header: Authorization
  secret: 2a10q42lY7Y18xqrFt1qbODZIO4OMTeOxnrCe7tF3n9bazJinVE7VH5Pi
  access-token-period: 1800000000000000000  # 30분 (30분 * 60초 * 1000밀리초)
  refresh-token-period: 1209600000000  # 14일 (14일 * 24시간 * 60분 * 60초 * 1000밀리초)
  reissue-token-period: 2592000000000  # 3일 (3일 * 24시간 * 60분 * 60초 * 1000밀리초)


#springdoc:
#    default-consumes-media-type: application/json
#    default-produces-media-type: application/json
#    api-docs:
#      groups:
#        enabled: true
#    swagger-ui:
#      operations-sorter: alpha # alpha(알파벳 오름차순), method(HTTP메소드순)
#      tags-sorter: alpha # 태그 정렬 기준
#      path: /member/swagger-ui.html # html 문서 접속 경로
#      disable-swagger-default-url: true
##      display-query-params-without-oauth2: true
#      doc-expansion: none # tag, operation 펼치는 방식
#    paths-to-match:
#      - /**